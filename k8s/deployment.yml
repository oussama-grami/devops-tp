apiVersion: apps/v1
kind: Deployment
metadata:
  name: devops-tp-app
  namespace: devops-tp
  labels:
    app: devops-tp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: devops-tp
  template:
    metadata:
      labels:
        app: devops-tp
    spec:
      containers:
      - name: devops-tp
        image: DOCKER_IMAGE_PLACEHOLDER
        ports:
        - containerPort: 3000
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
